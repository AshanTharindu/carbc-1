package TestCases;

import network.Node;

public class GenesisBlockCreationTest {
    public static void main(String[] args) {
        Node node = Node.getInstance();
        node.startNode();
    }
}
